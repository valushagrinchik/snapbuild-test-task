{
  "name": "snapbuild",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npx -y http-server public -p 8080 -c-1",
    "build": "rm -rf docs && cp -r public docs",
    "start": "npx -y http-server docs -p 8080 -c-1"
  },
  "devDependencies": {}
}
